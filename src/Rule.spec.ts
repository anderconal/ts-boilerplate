import { RuleInterface } from './FizzBuzz';

describe('Rules Testing', () => {
	it('IsDivisible ', () => {
    const number3 = 3;
		const ruleArray: RuleInterface[] = [];
		for (const rule of ruleArray) {
			if (rule.isDivisible(number3)) {

			}
		}

		return number;
	});
});